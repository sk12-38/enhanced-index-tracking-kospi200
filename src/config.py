"""
config.py
=========
Dai & Li (2024) 논문의 모든 하이퍼파라미터를 중앙 관리합니다.
논문 설정값과 정확히 일치시킵니다.
"""

# ── 데이터 경로 ─────────────────────────────────────────────────────────────
PRICE_PATH = "KOSPI200_adj_close.csv"
INDEX_PATH = "KOSPI200_index.csv"
MCAP_PATH  = "KOSPI200_mkt_cap.csv"

# ── 우주 구성 ────────────────────────────────────────────────────────────────
TOP_N_LIST       = [5]          # 실험할 편입 종목 수 목록
TOP_N_DEFAULT    = 5               # 기본 편입 종목 수
FULL_PERIOD_ONLY = True             # 훈련 전 기간 내 결측 없는 종목만 사용

# ── 거래비용 ─────────────────────────────────────────────────────────────────
RHO_LIST = [0.0, 0.005]             # 거래비용률 실험 목록

# ── 리밸런싱 / 레짐 ───────────────────────────────────────────────────────────
REBAL_FREQ     = 5                  # 리밸런싱 주기 (거래일)
REGIME_SMOOTH  = 2                  # HMM 레짐 이동평균 스무딩 윈도우

# ── 단기 통계 윈도우 ──────────────────────────────────────────────────────────
WINDOW_ST = 63                      # 63 거래일 ≈ 3개월

# ── EIT 손실 파라미터 ─────────────────────────────────────────────────────────
LAMBDA_EIT = 20                     # 초과수익 가중치 λ

# ── EIT-CVaR 손실 파라미터 ───────────────────────────────────────────────────
ALPHA_CVAR   = 0.05                 # CVaR 신뢰수준 (하위 5%)
C_CVAR       = 0.03                 # CVaR 허용 임계값 c
GAMMA_CVAR   = 1e6                  # 패널티 스케일 γ
BETA_SOFTPLUS = 2000                # Softplus 스케일 β

# ── 학습 파라미터 ─────────────────────────────────────────────────────────────
PATH_LEN    = 200                   # 부트스트랩 경로 길이 (거래일)
BATCH_SIZE  = 128                   # 미니배치 크기 (경로 수)
EPOCHS      = 50                    # 학습 에포크
LR          = 1e-2                  # Adam 학습률
BLOCK_SIZE  = 10                    # 블록 부트스트랩 블록 크기 (거래일)
VAL_RATIO   = 0.1                   # 검증 데이터 비율

# ── RO 베이스라인 파라미터 ────────────────────────────────────────────────────
RO_LOOKBACK = 2 * 252               # RO 사용 과거 데이터 길이 (≈2년)
RO_EPOCHS   = 200                   # RO 재최적화 에포크
RO_BLOCK    = 10                    # RO 블록 부트스트랩 블록 크기

# ── 롤링 백테스트 기간 ────────────────────────────────────────────────────────
TRAIN_START_YEAR = 2000             # 첫 훈련 시작 연도 (데이터: 2000~2022)
TEST_START_YEAR  = 2017             # 첫 테스트 연도
TEST_END_YEAR    = 2022             # 마지막 테스트 연도

# ── 정책 변형 (Policy Variants) ──────────────────────────────────────────────
# 논문 Table 1의 4가지 신경망 정책 변형
POLICY_VARIANT  = "NN-All"                         # 기본값: 전체 블록 사용
POLICY_VARIANTS = ["NN-ST", "NN-IR", "NN-ISR", "NN-All"]  # 모든 변형 목록

# ── 피처 정규화 ──────────────────────────────────────────────────────────────
# RollingBacktest(use_normalization=True) 시에만 사용됨
# 기본값: use_normalization=False → 기존 동작 보존
NORM_METHOD          = "train_zscore"   # 정규화 방법 (현재: train z-score)
NORM_EPS             = 1e-8             # sigma 하한 (division-by-zero 방지)
NORMALIZE_REGIME     = False            # regime prob (col 0) 정규화 여부
CHECKPOINT_DIR_NORM  = "checkpoints_norm"  # 정규화 on 체크포인트 저장 경로

# ── 재현성 ───────────────────────────────────────────────────────────────────
RANDOM_SEED = 42
